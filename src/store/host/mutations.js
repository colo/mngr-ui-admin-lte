
import Vue from 'vue'

// export const networkInterfaces = function(state, networkInterfaces) {
//   state.networkInterfaces = networkInterfaces
// }

export const charts = function(state, charts) {
  state.charts = charts
}

export const instances = function(state, instances) {
  state.instances = instances
}
